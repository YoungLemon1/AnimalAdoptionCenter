function ajaxpost() {
    window.alert("Animal created");
    //// (A) GET FORM DATA
    //var form = document.getElementById("newAnimalForm");
    //var data = new FormData(form);

    //// (B) AJAX REQUEST
    //// (B1) POST DATA TO SERVER, RETURN RESPONSE AS TEXT
    //fetch("C:\Users\yaniv\source\repos\Github\AnimalAdoptionCenter\master\V4\AnimalAdoptionCenter\Views\Animal\Index.cshtml", { method: "POST", body: data })
    //    .then(res => res.text())

    //    // (B2) SHOW MESSAGE ON SERVER RESPONSE
    //    .then((response) => {
    //        window.prompt(response);
    //        if (response == "OK") { alert("SUCCESSFUL!"); }
    //        else { alert("FAILURE!"); }
    //    })
}